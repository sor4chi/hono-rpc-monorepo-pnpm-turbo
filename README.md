# Hono RPC Monorepo with pnpm workspaces and turborepo

```bash
pnpm i
```

This project is made for Cloudflare Workers.
You can use `pnpm run deploy` under the each package directory.

```bash
cd apps/{client,server}
pnpm run deploy
```
